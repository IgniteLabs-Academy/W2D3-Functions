// Declare a function named greet with one parameter 'name'
// It should return a string like 'Hello, name!'

// Declare an arrow function version of greet

// Declare a function named add that takes two parameters and returns their sum

// Declare a function named multiply that takes two parameters and returns their product

// Declare a function named checkEven that takes one parameter and returns true if it is even, otherwise false

// Declare a function named calculateBMI that takes height (h) and weight (w) and returns the BMI

// Call each function with sample arguments and log the result using console.log
